{
  "$schema": "https://schema.tauri.app/config/2/capability",
  "identifier": "dapp-browser",
  "description": "Capability for the dApp browser window",
  "windows": ["dapp-browser"],
  "permissions": [
    "core:default",
    "core:window:allow-close",
    "core:window:allow-set-focus"
  ]
}
